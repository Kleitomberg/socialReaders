<form method="post" action="{{ path('app_postagem_delete', {'id': postagem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postagem.id) }}">
    <button class="btn">Delete</button>
</form>
