{% extends "base.html.twig" %}

{% block title %} SocialReaders | Amigos {% endblock %}


{% block body %}

<div class="container">

    <h1 class="mb-4 mt-5"> Amigos </h1>

    <div class="text-center  div-form-friends">
        <form method="post" action="{{path('app_serachamigos')}}" class="d-flex form-friends justfy-content-center" role="search" name="searchfriends">
            <div class="inp-bt d-flex">
            <input class="form-control search-amigos" name="friends" type="search" placeholder="Buscar Amigos" aria-label="Search">
            <button type="submit" class="btn btn-primary btn-buscar-friend"><i class="fa-solid fa-magnifying-glass"></i></button>
        </div>
        </form>
    </div>

    <div class="list-amigos mt-4 p-2 row gap-3 center ">

        {%if solicitacoes%}
        <h1>Solicitões de Amizade <span class=""> {{solicitacoes|length}}</span></h1>
        {% for s in solicitacoes %}


        <div class="card">
            <div class="card-body">
                Fulainho quer ser seu amigo
                <button class="btn-sm btn btn-danger"><i class="fa-solid fa-check"></i></button>
                <button class="btn-sm btn btn-success"><i class="fa-solid fa-xmark"></i></button>

            </div>

        </div>

{%endfor%}

        {%endif%}

        {%if frindssearch%}

        Foram enocntrados {{frindssearch|length}}

        {%for resultado in frindssearch%}

        <div class="card friends mt-3 mb-3">
            <div class="card-body gap-2">

                <div class="row friends-row">
                    <div class="col">
                        <img class="img-fluid profile " src="assets/images/profile2.jpg" alt="logo"></a>
                    </div>

                    <div class="col-md-9">

                        <p>{{resultado.nome}}</p>
                        <div class="d-flex gap-2">
                            <button class="btn btn-primary btn-sm">Mensagem</button>
                            <button class="btn btn-danger btn-sm"><i class="fa-solid fa-trash"></i></button>
                            <form class="d-flex" action="{{path('app_addamimgos')}}" method="post" name="amizade">
                            <input type="hidden" name="userid" value="{{resultado.id}}">
                            <button type="submit" name="amizade" class="btn btn-warning btn-sm"><i class="fa-solid fa-user-plus"></i></button>
                        </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {%endfor%}

        {%else%}
        {%if myfriends%}
        {% for amigo in myfriends%}

        <div class="card friends mt-3 mb-3">
            <div class="card-body gap-2">

                <div class="row friends-row">
                    <div class="col">
                        <img class="img-fluid profile " src="assets/images/profile2.jpg" alt="logo"></a>
                    </div>

                    <div class="col-md-9">

                        <p>{{amigo.nome}}</p>
                        <div>
                            <button class="btn btn-primary">Mensagem</button>
                            <button class="btn btn-danger"><i class="fa-solid fa-trash"></i></button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {%endfor%}
        {%else%}
        <p>Você ainda não tem amigos :(</p>
        {%endif%}



        {%endif%}

    </div>



</div>

{% endblock %}
